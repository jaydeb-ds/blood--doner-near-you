<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blood Donor Near Me</title>
    <link rel="stylesheet" href="styles.css">
    <style>
        /* Reset Styles */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}
body {
    font-family: 'Arial', sans-serif;
    line-height: 1.6;
    background: linear-gradient(to bottom, #f7f7f7, #eaeaea);
    color: #333;
}

/* Colors */
:root {
    --primary: #e63946; /* Crimson Red */
    --secondary: #1d3557; /* Deep Blue */
    --accent: #a8dadc; /* Soft Aqua */
    --light: #ffffff; /* White */
    --dark: #2d2d2d; /* Dark Grey */
}

/* Header */
.header {
    background: var(--primary);
    color: var(--light);
    padding: 1rem 0;
    position: sticky;
    top: 0;
    z-index: 1000;
    box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
}
.header .container {
    display: flex;
    justify-content: space-between;
    align-items: center;
    max-width: 1200px;
    margin: auto;
    padding: 0 1rem;
}
.header .logo {
    font-size: 1.8rem;
    font-weight: bold;
}
.header .logo span {
    color: var(--accent);
}
.header .nav a {
    color: var(--light);
    text-decoration: none;
    margin: 0 15px;
    font-size: 1rem;
    transition: color 0.3s ease;
}
.header .nav a:hover {
    color: var(--accent);
}

.below-nab{
    width: 100px;
    height: 60px;
}
/* Hero Section */
.hero {
    background: linear-gradient(to right, var(--primary), var(--secondary));
    color: var(--light);
    text-align: center;
    padding: 4rem 1rem;
}
.hero h2 {
    font-size: 2.5rem;
    margin-bottom: 1rem;
    animation: fadeIn 1.5s ease-out;
}
.hero p {
    font-size: 1.2rem;
    margin-bottom: 2rem;
    animation: slideUp 1.5s ease-out;
}
.hero .cta-btn {
    background: var(--accent);
    color: var(--dark);
    padding: 0.8rem 1.5rem;
    border-radius: 30px;
    border: none;
    font-size: 1rem;
    cursor: pointer;
    transition: transform 0.3s ease, background 0.3s ease;
}
.hero .cta-btn:hover {
    background: var(--dark);
    color: var(--light);
    transform: scale(1.1);
}

/* Sections */
section {
    padding: 3rem 1rem;
    text-align: center;
}
.about, .donate, .contact {
    background: var(--light);
    color: var(--dark);
    border: 2px solid var(--accent);
    border-radius: 10px;
    margin: 2rem auto;
    max-width: 800px;
    padding: 2rem;
}
.donate {
    background: var(--accent);
    color: var(--dark);
}
.contact {
    background: var(--secondary);
    color: var(--light);
}
section h2 {
    font-size: 2rem;
    margin-bottom: 1rem;
}
section p {
    font-size: 1rem;
    margin-bottom: 1.5rem;
}
section .cta-btn {
    text-decoration: none;
    background: var(--primary);
    color: var(--light);
    padding: 0.7rem 1.5rem;
    border-radius: 30px;
    border: none;
    cursor: pointer;
    transition: background 0.3s ease, transform 0.3s ease;
}
section .cta-btn:hover {
    background: var(--accent);
    transform: scale(1.1);
}

/* Footer */
.footer {
    background: var(--dark);
    color: var(--light);
    text-align: center;
    padding: 1rem;
}

/* Animations */
@keyframes fadeIn {
    from {
        opacity: 0;
    }
    to {
        opacity: 1;
    }
}
@keyframes slideUp {
    from {
        transform: translateY(50px);
        opacity: 0;
    }
    to {
        transform: translateY(0);
        opacity: 1;
    }
}

/* Responsive Design */
@media (max-width: 768px) {
    .header .container {
        flex-direction: column;
    }
    .hero h2 {
        font-size: 2rem;
    }
    .hero p {
        font-size: 1rem;
    }
    section {
        padding: 2rem 1rem;
    }
}

    </style>
</head>
<body>
    <!-- Header Section -->
    <header class="header">
        <div class="container">
            <h1 class="logo">Blood<span>Donor</span></h1>
            <nav class="nav">
                <a href="#home">Home</a>
                <a href="#about">About</a>
                <a href="#contact">Donate</a>
                <a href="#home">Doner</a>
            </nav>
        </div>
    </header>
  
    <!-- Hero Section -->
    <section id="home" class="hero">
        <div class="container">
            <h2>Find Blood Donors Near You</h2>
            <p>Your small step can save a life. Join us and become a hero today!</p>
    <!-- <a href="/about" class="cta-btn" >doner near me</a> -->
    <!-- <button class="cta-btn"> Find Donors</button> -->
        </div>
    </section>

     <!-- Donate Section -->
     <section id="donate" class="donate">
        <div class="container">
            <h2>Doner near you</h2>
            <p>Find the required blood group doner near you and contant directly with doner. Fill the required information and get the doner in your area. <br> go and click here</p>
        
            <a href="/view_doner" class="cta-btn" >Find Donor</a>
        </div>
    </section>

    <!-- About Section -->
    <section id="about" class="about">
        <div class="container">
            <h2>About Us</h2>
            <p>We connect individuals in need of blood with donors ready to help. Together, we save lives and create hope.</p>
        </div>
    </section>

   

    <!-- Contact Section -->
    <section id="contact" class="contact">
        <div class="container">
            <h2>How to dontate</h2>
            <p>Become a donor by registering on our platform. We will notify you whenever there’s a need near you.</p>

    <a href="/register" class="cta-btn" >become a doner</a>
         <!-- <button class="cta-btn">Get in Touch</button> -->
        </div>
    </section>

    <!-- Footer -->
    <footer class="footer">
        <div class="container">
            <p>&copy; 2024 Blood Donor Near Me. All Rights Reserved.</p>
        </div>
    </footer>
</body>
</html>
